mutation {
    updateModule(module: { id: "id", title: "Title of module", type: "ICONS" }) {
        id
        module {
            id,
            title,
            type
        }
        status {
            status
            message
        }
    }
}