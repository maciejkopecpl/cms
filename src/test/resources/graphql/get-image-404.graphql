query {
    image(id: "404_ID") {
        __typename
        ... on Image {
            id
            image
            filename
        },
        __typename ... on Status {
            status,
            message
        }
    }
}