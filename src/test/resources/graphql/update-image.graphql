mutation {
    updateImage(image: { id: "id", image: "image_id", filename: "filename" }) {
        id
        image {
            id,
            image,
            filename
        }
        status {
            status
            message
        }
    }
}