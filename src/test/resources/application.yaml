spring:
  data:
    mongodb:
      port: 0 # use random value
  main:
    allow-bean-definition-overriding: true
  mail:
    host: smtp.host.com

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.4
graphql:
  servlet:
    websocket:
      enabled: false
application:
  mail:
    to: fake@mail.com
    subject: "test"
  recaptcha-secret: "FAKE_SECRET"
  allowed-origins: "*"
  api-key: "FAKE_API_KEY"
